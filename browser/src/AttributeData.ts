import Attribute from "./model/Attribute";
import R from "./R";

const data = {};

data[Attribute.COLD] = {
    name: R.cold_attribute_name,
    description: R.cold_attribute_description,
};

data[Attribute.RAIN] = {
    name: R.rain_attribute_name,
    description: R.rain_attribute_description,
};

data[Attribute.FOG] = {
    name: R.fog_attribute_name,
    description: R.fog_attribute_description,
};

data[Attribute.GOOD_WEATHER] = {
    name: R.good_weather_attribute_name,
    description: R.good_weather_attribute_description,
};

data[Attribute.COMMANDERS_HORN] = {
    name: R.commanders_horn_attribute_name,
    description: R.commanders_horn_attribute_description,
};

data[Attribute.HERO] = {
    name: R.hero_attribute_name,
    description: R.hero_attribute_description,
};

data[Attribute.MEDIC] = {
    name: R.medic_attribute_name,
    description: R.medic_attribute_description,
};

data[Attribute.SPY] = {
    name: R.spy_attribute_name,
    description: R.spy_attribute_description,
};

data[Attribute.SUPPLY] = {
    name: R.supply_attribute_name,
    description: R.supply_attribute_description,
};

data[Attribute.BOND] = {
    name: R.bond_attribute_name,
    description: R.bond_attribute_description,
};

data[Attribute.SCOURGE] = {
    name: R.scourge_attribute_name,
    description: R.scourge_attribute_description,
};

export default data;
